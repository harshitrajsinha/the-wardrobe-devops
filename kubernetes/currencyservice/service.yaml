apiVersion: v1
kind: Service
metadata:
  name: currencyservice-svc
  labels:
    app: currencyservice
  namespace: currencyns
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: currencyservice-app
  ports:
  - name: grpc
    port: 7000
    targetPort: 7000